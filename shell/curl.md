# curl

```shell
$ curl --location https://example.com               # Follow redirection if response to request is 3xx
$ curl --remote-name https://example.com/file.txt  # Write output to file and keep filename
$ curl --I https://example.com                     # Get header only
$ curl -x GET https://example.com                  # specify HTTP method
```
